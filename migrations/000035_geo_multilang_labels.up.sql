-- Мультиязычные подписи точек (запрос владельца, 2026-07-12): адрес,
-- показываемый в карточке, должен быть на языке интерфейса, а не на языке
-- подачи (китаец не читает «Хоргос»). Координаты уже храним; при постановке
-- точки фронт делает обратное геокодирование на ru/en/zh и кладёт JSON-карту
-- подписей. Показ выбирает нужный язык (fallback: язык → en → исходная
-- подпись). Когда подключим Amap — китайскую подпись для точек в Китае будем
-- брать из него, структура хранения (jsonb) не меняется.
ALTER TABLE vehicles
    ADD COLUMN location_labels jsonb;

ALTER TABLE vehicle_destinations
    ADD COLUMN labels jsonb;
