-- Тарифные планы (ТЗ §19.5): у набора инструментов появляется цена в
-- тенге за месяц. 0 = бесплатный тариф. Реальное списание — вместе с
-- платёжным провайдером; цена пока информационная (карточки тарифов).
ALTER TABLE permission_sets ADD COLUMN price_kzt numeric NOT NULL DEFAULT 0 CHECK (price_kzt >= 0);
